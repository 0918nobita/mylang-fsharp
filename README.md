# Mylang

## ビルド・実行

```bash
$ dotnet restore
$ dotnet tool restore
$ dotnet run --project src/App source.mylang
```

## ドキュメント生成

```bash
$ dotnet fsdocs build

# Watch mode
$ dotnet fsdocs watch
```
